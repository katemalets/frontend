import { Auth.Interceptor.Ts } from './auth.interceptor.ts';

describe('Auth.Interceptor.Ts', () => {
  it('should create an instance', () => {
    expect(new Auth.Interceptor.Ts()).toBeTruthy();
  });
});
